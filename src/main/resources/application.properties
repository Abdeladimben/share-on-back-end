jwt.secret = java

/////  86400000 = jour
jwt.JWT_TOKEN_VALIDITY=86400000   
jwt.refreshExpirationDateInMs=180000

server.port=8090
spring.datasource.url=jdbc:mysql://localhost:3306/share_on?useSSL=false
spring.datasource.username=root
spring.datasource.password=123456

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.hibernate.ddl-auto = update

spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER

app.jwt.keystore-location=key/keystore.jks
app.jwt.keystore-password=shareon
app.jwt.key-alias=key
app.jwt.private-key-passphrase=shareon

#keytool -genkeypair -alias *** -keyalg RSA -keypass *** -keystore URL -storepass *** 

